{
    "tasks": [
        {
            "label": "make debug",
            "type": "shell",
            "command": "make debug",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/c_block_sparse_format"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}